export enum SocketEvents {
  DRAW = "draw:line",
  CLEAR = "draw:clear",
  USER_JOIN = "user:join",
  USER_LEAVE = "user:leave",
  INIT_CANVAS = "canvas:init",
  REQUEST_HISTORY = "canvas:request_history",
}
